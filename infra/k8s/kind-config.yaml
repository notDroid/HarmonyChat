apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
name: harmony
nodes:
  - role: control-plane
    extraPortMappings:
      # Backend FastAPI
      - containerPort: 30800
        hostPort: 8000
        protocol: TCP
