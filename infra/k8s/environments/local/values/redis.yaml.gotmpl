fullnameOverride: {{ .Values.infra.redis.host | replace "-master" "" }}

architecture: standalone

auth:
  enabled: false

master:
  persistence:
    enabled: true
    size: 1Gi
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi