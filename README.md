# SimpleDiscord
A simple Discord-like chat application backend built with FastAPI and DynamoDB.

### Setup
Dependencies:
- conda
- taskfile
```Bash
conda env create -n simple-discord
conda activate simple-discord
python -m pip install -e .
```

Run locally:
```Bash
task run-local
```